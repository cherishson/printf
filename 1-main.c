#include "main.h"
#include <stdio.h>

int main(void)
{
	_printf("Bright is %c even at %s aand %% and is faithful\n", 'H', "old age");
	printf("bright is %c even at %s and %% and is faithful\n", 'H', "old age");

	return (0);
}
